<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Debug builds only: allow plain-HTTP to a dev machine on the LAN. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
